# Server block for tcalendar-side-service.linagora.local
server {
    listen 80;
    server_name tcalendar-side-service.linagora.local;

    location / {
        proxy_hide_header 'Access-Control-Allow-Origin';

        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, PATCH, DELETE, REPORT, PROPFIND, PROPPATCH, MKCOL, COPY, MOVE, LOCK, UNLOCK, MKWORKSPACE, UPDATE, CHECKIN, CHECKOUT, UNCHECKOUT, VERSION-CONTROL, SEARCH, LABEL' always;
            add_header 'Access-Control-Allow-Headers' 'Origin, Content-Type, Authorization' always;
            add_header 'Access-Control-Allow-Origin' "*" always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header Expires 0;
            add_header Cache-Control "no-store, no-cache, must-revalidate, max-age=0";
            return 204;
        }

        if ($request_method != 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' "*" always;
            add_header 'Access-Control-Allow-Credentials' 'true';
        }

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_pass http://tcalendar-side-service:80;
    }
}

server {
    listen 443 ssl;
    server_name tcalendar-side-service.linagora.local;

    ssl_certificate     /etc/nginx/certs/linagora.local.pem;
    ssl_certificate_key /etc/nginx/certs/linagora.local-key.pem;

    location / {
        proxy_hide_header 'Access-Control-Allow-Origin';

        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, PATCH, DELETE, REPORT, PROPFIND, PROPPATCH, MKCOL, COPY, MOVE, LOCK, UNLOCK, MKWORKSPACE, UPDATE, CHECKIN, CHECKOUT, UNCHECKOUT, VERSION-CONTROL, SEARCH, LABEL' always;
            add_header 'Access-Control-Allow-Headers' 'Origin, Content-Type, Authorization' always;
            add_header 'Access-Control-Allow-Origin' "*" always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header Expires 0;
            add_header Cache-Control "no-store, no-cache, must-revalidate, max-age=0";
            return 204;
        }

        if ($request_method != 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' "*" always;
            add_header 'Access-Control-Allow-Credentials' 'true';
        }

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_pass http://tcalendar-side-service:80;

        proxy_connect_timeout       60s;
        proxy_send_timeout          60s;
        proxy_read_timeout          60s;
        send_timeout                60s;
    }
}

server {
    listen 443 ssl;
    server_name calendar.linagora.local;
    ssl_certificate     /etc/nginx/certs/linagora.local.pem;
    ssl_certificate_key /etc/nginx/certs/linagora.local-key.pem;

    location /api {
        proxy_pass http://tcalendar-side-service:80;
    }

    location ~ ^/linagora\.esn\.dav\.import/api {
        proxy_pass http://tcalendar-side-service.linagora.local:80;
    }

    location / {
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' '*';
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, PATCH, DELETE';
            add_header 'Access-Control-Max-Age' 1728000;
            return 204;
        }

        add_header 'Referer-Policy' 'no-referrer-when-downgrade';
        proxy_pass http://calendar.linagora.local:80;
    }
}

server {
    listen 443 ssl;
    server_name excal.linagora.local;
    ssl_certificate     /etc/nginx/certs/linagora.local.pem;
    ssl_certificate_key /etc/nginx/certs/linagora.local-key.pem;

    location /api {
        proxy_pass http://tcalendar-side-service:80;
    }

    location ~ ^/linagora\.esn\.dav\.import/api {
        proxy_pass http://tcalendar-side-service.linagora.local:80;
    }

    location / {
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' '*';
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, PATCH, DELETE';
            add_header 'Access-Control-Max-Age' 1728000;
            return 204;
        }

        add_header 'Referer-Policy' 'no-referrer-when-downgrade';
        proxy_pass http://excal.linagora.local:80;
    }
}

server {
    listen 443 ssl;
    server_name contacts.linagora.local;
    ssl_certificate     /etc/nginx/certs/linagora.local.pem;
    ssl_certificate_key /etc/nginx/certs/linagora.local-key.pem;

    location /api {
        proxy_pass http://tcalendar-side-service:80;
    }

    location /contact/app/contact/action/copy/contact-action-copy-dialog.html {
        proxy_pass http://tcalendar-side-service:80;
    }

    location /contact/app/contact/action/move/contact-action-move-dialog.html {
        proxy_pass http://tcalendar-side-service:80;
    }

    location / {
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' '*';
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, PATCH, DELETE';
            add_header 'Access-Control-Max-Age' 1728000;
            return 204;
        }

        add_header 'Referer-Policy' 'no-referrer-when-downgrade';
        proxy_pass http://contacts.linagora.local:80;
    }
}

server {
    listen 443 ssl;

    server_name account.linagora.local;
    ssl_certificate     /etc/nginx/certs/linagora.local.pem;
    ssl_certificate_key /etc/nginx/certs/linagora.local-key.pem;

    location / {
        proxy_pass http://account.linagora.local:80;
    }
}

server {
    listen 443 ssl;
    server_name sabre-dav.linagora.local;
    ssl_certificate     /etc/nginx/certs/linagora.local.pem;
    ssl_certificate_key /etc/nginx/certs/linagora.local-key.pem;

    location / {
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' $http_origin always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, PATCH, DELETE, REPORT, PROPFIND, PROPPATCH, MKCOL, COPY, MOVE, LOCK, UNLOCK, MKWORKSPACE, UPDATE, CHECKIN, CHECKOUT, UNCHECKOUT, VERSION-CONTROL, SEARCH, LABEL, ACL' always;
            add_header 'Access-Control-Allow-Headers' $http_access_control_request_headers always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            return 204;
        }

        proxy_pass http://sabre-dav.linagora.local:80;
        proxy_set_header Host $host;

        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Credentials;

        add_header 'Access-Control-Allow-Origin' $http_origin always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
    }
}

server {
    listen 443 ssl;
    server_name sso.linagora.local;

    ssl_certificate     /etc/nginx/certs/linagora.local.pem;
    ssl_certificate_key /etc/nginx/certs/linagora.local-key.pem;

    location / {
        proxy_pass http://sso.linagora.local:5556;
        proxy_http_version 1.1;

        proxy_set_header Host               $host;
        proxy_set_header X-Real-IP          $remote_addr;
        proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto  https;

        proxy_pass_request_headers on;
        proxy_set_header Cookie $http_cookie;
    }
}
